target

# Secrets - NEVER commit these
secrets.env
.env
*.pem
*.key

# Vault runtime data (unseal keys, tokens)
config/vault/init.json
config/vault/approle.env
scripts/vault-seed.sh

# Logs
*.log

# macOS
.DS_Store

# LaunchAgent plists (may contain secrets)
scripts/*.plist