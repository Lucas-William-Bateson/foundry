[build]
dockerfile = "Dockerfile"
context = "."
command = "npm run build && npm test"
timeout = 1800

[triggers]
branches = ["main", "master", "develop"]
pull_requests = true

[deploy]
name = "my-app"
domain = "myapp.l3s.me"
port = 3000

[env]
NODE_ENV = "production"
